import { Text, StyleSheet, SafeAreaView, View } from 'react-native';

const HomeScreen = () => {
  return (
    <SafeAreaView>
      <Text style={{ color: '#ffffff'}}>Index/Home Page</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
});

export default HomeScreen;